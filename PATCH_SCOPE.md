# 패치 범위 — v.0.9.0

## 1. 텍스트 번역

- 총 활성 번역 행: `40,218 / 40,218`
- EVD 이벤트 텍스트: `25,346`행
- ELF 내장 텍스트: `14,872`행
- 활성 번역 누락: `0`행
- 분할 원고 기준 누락 ID: `0`, 중복 ID: `0`, 원본 순서 변경: `0`
- 전역 용어 규칙 검사: `9,741 / 9,741` 통과

잠금·제외된 ELF `122`행은 실행 제어, 포맷 데이터, 기술 문자열처럼 번역 대상으로 다루면 안 되는 항목입니다. 활성 일본어 대사 누락으로 집계하지 않습니다.

## 2. 폰트와 대사 레이아웃

- 한국어 비트맵 폰트 `3,566`글리프 적용
- Noto Sans KR `Medium 500` 기반 굵기
- 원본 일본어/영문 글리프와 한국어 글리프의 공존 처리
- 화자명 아틀라스 한국어화 및 선명도 보정
- 비정상 전각 공백과 번역 과정에서 생긴 불필요한 띄어쓰기 정리
- 대사창 폭을 기준으로 줄바꿈 재배치 및 오버플로 검사
- 고정 슬롯을 넘는 문자열은 안전 영역 재배치 방식으로 처리

## 3. 이미지형 UI

확정된 이미지 컨테이너 `109`개를 대상으로 작업했습니다.

- 감사 결과: `108`개 통과, `1`개 해당 없음
- 잠긴 원본 이미지: `109 / 109`
- 확인한 텍스트 영역: `897`
- 원본 이미지 해시 불일치: `0`
- 전투, 상태, 메인 메뉴, 상점, 지도, 설정, 다이브, 타이틀, 도움말 계열 포함
- 이미지에 일본어 글자를 단순히 덮지 않고, 원문 글자 영역을 제거한 뒤 배경·알파를 복원하고 한국어를 다시 조판
- 캔버스 크기, 비문자 픽셀, 팔레트 동작과 컨테이너 크기를 보존
- 재삽입 후 TIM/TM2/LZR/FP/RPK 왕복 무결성 검사

추가 보정:

- 상태/메뉴 글자 아틀라스 `5`개 선명도 재조정
- ELF에 패킹된 하단 조작 범례 `56`개 한국어화
- 감사 대상 하단 범례의 일본어 잔존 `0`

## 4. 안정성 보수

- 새 게임 시작 후 정지 문제 보수
- 인카운트 도움말 이후 R5900 예외·프리징 관련 손상 범위 보수
- 프리징 방지용 수정 범위 `9`곳 보존 여부 재검사
- 최종 구조 감사:
  - RPK 엔트리 `2,805`
  - FP 아카이브 `671`
  - FP 멤버 `9,030`
  - 이벤트 파일 `976`
  - 구조 오류 `0`

CPU 설정 변경은 패치에 포함하지 않습니다.

## 5. 제외 및 미완료 범위

`v.0.9.0`은 릴리스 후보 성격의 시험판입니다.

- 게임 전체 구간의 처음부터 끝까지 실기 플레이 검수 미완료
- 동영상 또는 사전 렌더링 연출에 박힌 일본어 자막의 완전 한국어화 미보장
- 이미지 텍스트 여부가 불확실한 후보 `7`개는 확정 작업 범위에서 제외
- 문맥, 고유명사, 선택지 길이와 특수 상황의 줄바꿈은 후속 실기 검수에서 추가 조정될 수 있음

## 6. 무결성 기준

| 구분 | SHA-256 |
|---|---|
| 지원 원본 ISO | `E35BAA27F64B60F05E9ABB532B89FCE19AEDC1A3466C0D5329C77359BCC8CB0E` |
| 패치 결과 ISO | `5A489C74B8B98B9CF5AC79733C8A6C3FF35D74A84EBB43A097F6CE73FD5A954C` |
| v.0.9.0 xdelta | `38182D149EC54A4896AC89C0F6745EEC9C4C5BB5C6893D98B4487535C49716B5` |

xdelta3 `3.1.0` 역적용 검증으로 결과 ISO 전체 바이트 일치를 확인했습니다.
